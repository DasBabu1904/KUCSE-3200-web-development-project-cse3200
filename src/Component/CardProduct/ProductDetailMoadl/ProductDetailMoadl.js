import React from 'react';
import Button from 'react-bootstrap/Button';
import Modal from 'react-bootstrap/Modal';

const ProductDetailMoadl = (props) => {
    return (
        <h1> Here we are</h1>
      );
};

export default ProductDetailMoadl;

